@model IEnumerable<fitness.Models.Abouts>

@{
    ViewBag.Title = "About";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="about_section_2 layout_padding">
    <div class="container">
        @if (Model != null)
        {
            foreach (var about in Model)
            {
                <h1 class="about_text_2">@about.Title</h1>
            }
        }
        else
        {
            <p>No data available</p>
        }
    </div>
</div>

<div class="about_section about_main">
    <div class="row">
        <div class="col-md-6">
            <div class="about_taital">
                @if (Model != null)
                {
                    foreach (var about in Model)
                    {
                        <h1 class="about_text" style="color: deepskyblue">@about.PopularTitle</h1>
                        <p style="color: powderblue; font-weight: 500; font-size: larger; margin-left: 2px;">@about.Description</p>
                    }
                }
            </div>
        </div>
        <div class="col-md-6">
            @if (Model != null)
            {
                foreach (var about in Model)
                {
                    <div class="about_img"><img src="@about.Image1URL" /></div>
                }
            }
        </div>
    </div>
</div>






@*@model IEnumerable<fitness.Models.Abouts>



    <div class="about_section_2 layout_padding">
        <div class="container">
            @foreach (var abouts in Model)
            {
                <h1 class="plan_text">@abouts.Title </h1>
            }
            <h1 class="about_text_2"><strong>HAKKIMIZDA</strong></h1>
        </div>
    </div>

    <div class="about_section about_main">
        <div class="row">
            <div class="col-md-6">
                <div class="about_taital">
                    <h1 class="about_text" style="color:deepskyblue">SPOR SALONUMUZ HAKKINDA</h1>
                    <p style="color:powderblue;font-weight:500;font-size:larger; margin-left:2px;">FitClub, sağlıklı yaşam ve aktif bir yaşam tarzını benimseyen bireylere özel bir spor deneyimi sunan bir spor salonudur. Modern ve donanımlı tesisimizde, uzman antrenörler eşliğinde, kişisel hedeflerinize ulaşmanıza yardımcı olan çeşitli fitness programları ve egzersiz seçenekleri bulunmaktadır. Müşteri odaklı hizmet anlayışımız ve sıcak atmosferimizle, FitClub olarak her bir üyemizi destekleyerek, motivasyonunu artırmayı ve sağlıklı bir yaşam tarzını benimsemeyi hedefliyoruz.</p>

                </div>
            </div>
            <div class="col-md-6">
                <div class="about_img"><img src="~/Content/img/about-bg.png"></div>
            </div>
        </div>
    </div>*@
